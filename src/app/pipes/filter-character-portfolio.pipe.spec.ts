import { FilterCharacterPortfolioPipe } from './filter-character-portfolio.pipe';

describe('FilterCharacterPortfolioPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCharacterPortfolioPipe();
    expect(pipe).toBeTruthy();
  });
});
