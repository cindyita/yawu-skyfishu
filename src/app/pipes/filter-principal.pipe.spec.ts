import { FilterPrincipalPipe } from './filter-principal.pipe';

describe('FilterPrincipalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPrincipalPipe();
    expect(pipe).toBeTruthy();
  });
});
